# CorbeauSplat — Project Manifest

> Version 2.0.0-beta.4 — macOS Apple Silicon Gaussian Splatting Pipeline

## Identity

- **Purpose**: All-in-one GUI + CLI tool for Gaussian Splatting 3D reconstruction on macOS
- **Author**: Frederick (freddewitt) — github.com/freddewitt/CorbeauSplat
- **License**: MIT
- **Python**: 3.13+ (main), 3.11 (ML Sharp venv)
- **Stack**: PySide6, COLMAP/Glomap, Brush (Rust/WGPU), Apple ML Sharp, upscayl-ncnn, opencv-python-headless
- **File count**: ~78 Python files

## Quickstart

```bash
# GUI mode (default, no args)
python3 main.py

# CLI modes
python3 main.py pipeline -i video.mp4 -o ~/projects --preset dense
python3 main.py colmap -i images/ -o ~/projects
python3 main.py brush -i dataset/ -o dataset/ --preset dense
python3 main.py view -i splat.ply
python3 main.py upscale -i image.png -o ~/out --scale 4
python3 main.py clean -i splat.ply --strength strong
python3 main.py clean -i noisy.ply -o cleaned.ply --then-export spz
```

## Architecture

Vue d'ensemble : `main.py` (entry) → `app/cli/` (dispatch CLI) et `app/gui/` (PySide6, `studio_window.py`/`StudioWindow` orchestrateur unique — interface à 3 zones : rail gauche, centre, barre d'activité (`activity_bar.py` — étape en cours, détail, progression, Annuler ; le journal a sa propre fenêtre, `logs_window.py`) — + `panels/` par module) s'appuient tous deux sur `app/core/` (moteurs, tous héritant de `base_engine.py`/BaseEngine : `engine.py`/ColmapEngine, `brush_engine.py`, `sharp_engine.py`, `upscale_engine.py`, `superplat_engine.py`, `four_dgs_engine.py`, `extractor_360_engine.py`, `export_engine.py`, `ply_cleaner.py`, `splat_transform_engine.py`). `colmap_commands.py` isole la construction des argv COLMAP (SRP, extrait de ColmapEngine — v1.2.3). `media.py` centralise les formats acceptés — conteneurs vidéo (FFmpeg) et images, avec la conversion automatique des formats non lisibles par toute la chaîne (HEIC/TIFF/BMP/WebP… → PNG ou JPEG) ; les trois sites de détection (panneau Source, ColmapEngine, FourDGSEngine) y puisent la même liste. `app/scripts/installers/` installe les binaires externes dans `engines/`.

Le rail gauche est une **hiérarchie statique à trois niveaux, sans aucun repli/dépli** : « Projet » seul en tête (clé interne `source`, style permanent distinct — fond teinté + liseré d'accent), puis **ENTRAÎNEMENT** (reconstruction, entraînement, visualiser — le cœur du pipeline) et **OPTIONS** (upscale, nettoyage, export, extraction 360 — traitements optionnels chaînés au pipeline via des drapeaux type `upscaler_avant` ; à l'exécution, extraction 360 et upscale précèdent reconstruction, `PIPELINE_STEPS`/`plan_pipeline()` font foi pour la séquence réelle), tous deux présentés en arbre indenté relié à Projet par un filet vertical, et **OUTILS** (5 modules : brush, sharp, supersplat, splattransform, 4dgs — l'entrée « 360 Extractor » a été retirée en 2026-08-28, redondante avec OPTIONS/extraction360) séparé par un filet horizontal pleine largeur — groupe indépendant, pas un enfant de Projet. Les 14 items sont toujours visibles, chacun avec une icône unique (vérifiée présente dans la police via `QFontMetrics.inFont()`) ; les en-têtes de groupe sont des `QLabel` non cliquables. La barre supérieure fonctionnelle (`topbar.py`) a été supprimée : mode pipeline et bouton Lancer/Annuler vivent dans le panneau Projet, le bouton ⚙ (glyphe + libellé) dans la barre du bas. Un bandeau purement décoratif la remplace en tête de fenêtre (`app/gui/appbar.py` — icône + nom + version, aucune action). Le bouton Lancer est la seule action primaire accentuée de l'app : `objectName="btn_primary"` + règles `$accent`/`$highlight_text`/`$bright` dans `_STYLESHEET`, donc correct sur les 3 thèmes ; `set_running()` bascule une propriété Qt dynamique au lieu de réécrire la feuille de style. Les panneaux Paramètres n'ont plus de barre de droite (leur `_build_right()` retourne un `QWidget` sans layout) ; `StudioWindow` masque la colonne droite et son filet par détection structurelle, pas par liste de clés.

Détail (fichiers, classes, patterns, moteurs, dépendances, sécurité) → `graphify query "<question>"` ou `graphify explain "<concept>"`. Ne pas dupliquer ici ce que le graphe retrouve déjà.

## Known Issues & Gaps

 1. **E2E réel = pipeline principal seulement** — `pytest -m e2e` couvre COLMAP → Brush → clean → export SPZ (7 tests, ~21 s). Upscale/Sharp couverts en e2e réel (8 tests, ~145 s) ; 360/4DGS restent mockés. Suite par défaut : 676 pass, 2 skip (2026-09-20), dont 2 fichiers lançant un PySide6 **réel en sous-processus** (`test_gui_config_roundtrip.py`, `test_gui_panel_state_roundtrip.py`) — le mock PySide6 de `conftest.py` est global à la session et irréversible, donc un vrai panneau ne peut être instancié qu'ainsi. Marqueurs e2e : `e2e_sharp`, `e2e_upscale`, `e2e_4dgs`, `e2e_360` en plus de `e2e` umbrella (e2e désélectionné). GLB export tests skip si `trimesh`/`open3d` absents
 2. **Workers tested headless via mock PySide6** — `conftest.py` patches PySide6 at session scope, but import chain still requires numpy mock for CI

## Changelog Highlights

| Version | Key Changes |
|---------|-------------|
| **2.0.0-beta.4** (2026-09-17) | Audit dépendances tierces (checksums.json re-hashé, flags splat-transform --summary→--stats + --filter-floaters, contrainte --decimate/.ply) ; bouton Lancer local onglet Entraînement ; fix chaînage Reconstruction→Brush (checkbox « Run Brush » ignorée par le bouton local) ; fix Brush archivage (déplaçait tout le dossier parent au lieu des seuls checkpoints) ; fix 4DGS COLMAP (SfM tournait sur chaque frame de chaque caméra au lieu d'un snapshot synchronisé unique) ; fix launcher (`setuptools`/`wheel` absents du venv neuf faisaient échouer le build de paquets sans wheel précompilé, ex. pyobjc). |
| **2.0.0-beta.3** (2026-08-28) | **4DGS** : checkbox « Upscaler avant reconstruction » (upscale des dossiers `images/cam_XX` via le moteur Upscale partagé, avant COLMAP/`ns-process-data`) ; ancien bouton dédié « COLMAP seulement » remplacé par une checkbox intégrée au bouton Lancer unique ; fix fichiers AppleDouble macOS (`._*`) traités à tort comme vidéos ; `run_colmap()` accepte `camera_model`/`single_camera`/`matcher_type`/`sequential_overlap` (pas encore exposés en GUI) ; nouvel installeur `FourDGSEngineDep` pour `.venv_4dgs`. **Rail** : PARAMÈTRES scindé en ENTRAÎNEMENT/OPTIONS ; entrée OUTILS « 360 Extractor » retirée (redondante). i18n : 1 clé ajoutée, 1 scindée en 2, 1 retirée. **460 tests pass.** |
| **2.0.0-beta.2** (2026-08-24) | **Upscale engine** : GPU memory crash fix (SIGBUS on Apple Silicon, tile trop grand) ; PNG format normalization (16-bit, palette) ; input file protection (staging with model/scale suffix prevents overwrites when output=input folder) ; QThread re-entrancy safety (double-click crash). **UI fixes** : Upscale panel now allows selecting single image files (not just folders) ; `PIL.Image.MAX_IMAGE_PIXELS` disabled globally for large photogrammetry/drone images. i18n : 4 new keys. **456 tests pass.** |
| **2.0.0-beta.1** (2026-08-07) | Refonte Studio (Projet au centre, PARAMÈTRES/OUTILS, TopBar supprimée) ; **Upscale** et **Extraction 360** promus étapes du pipeline (`PIPELINE_STEPS` 6 → 8), chaînées par les drapeaux `upscaler_avant`/`source_360`, avec un canal unique `_pipeline_images_dir` pour propager le dossier d'images entre étapes ; chaînage réel Entraînement → Nettoyage → Export → Visualiser ; câblage des champs `checkpoint_dest`/`export_dir`/`export_format` (jusque-là sans effet) ; suppression de presets Brush et de configurations nommées depuis l'UI. i18n 283 clés × 9 locales. **447 tests + 45 d'intégration.** Validation manuelle GUI en attente (cf. RESTE À FAIRE §7). |
| **1.2.2** (2026-07-06) | SfM COLMAP : défaut **SIFT + DSP-SIFT** (`estimate_affine_shape=True`, force CPU) ; `vocab_tree` fonctionnel (était un no-op silencieux) ; `loop_detection` séquentiel (SIFT) ; **repli auto** `global_mapper`→`mapper` incrémental si pas de `sparse/0` valide (`_has_valid_sparse_model`). GUI responsive (5 onglets `QScrollArea`). `adapt_max_splats` : branche thermique morte corrigée (fair/serious/critical). Sécurité : `delete_project_content` bloque `/`, `$HOME`, dossier app + ancêtres. Réparation suite de tests (gel infini `readline`/EOF, pollution `cv2` session-wide) → **262 pass, 2 skip**. |
| **1.2.1** (2026-07-06) | Fix timeout Brush 3600s → 14400s configurable (`training_timeout`) ; `inactivity_timeout` dans `BaseEngine._execute_command()` (désactivé pour Brush — phases silencieuses). Rétrocompatible. |
| **1.2.0** (2026-07-05) | ExportTab async confirmé ; CI headless worker tests débloqués ; tests d'intégration étendus ; `pyproject.toml` → 1.2.0 |

_Versions ≤ 1.0.6 : voir `CHANGELOG.md` (historique complet)._

## Pipeline Flow

```
Input (Video/Images)
  → [Extraction 360] (étape PARAMÈTRES, si `source_360` — produit un NOUVEAU
     dossier d'images planaires que les étapes suivantes consomment)
  → [Upscale] (étape PARAMÈTRES, si `upscaler_avant` — source images uniquement ;
     pour une source vidéo *non* passée par l'extraction 360, l'upscale est
     délégué au moteur COLMAP, cf. ci-dessous)
  → [Extractor360] (if 360 mode)
  → [FFmpeg frame extraction] (if video)
  → [Blurry image filter] (if enabled)
  → [Upscale] (in-engine, si source vidéo)
  → [COLMAP feature_extractor / matcher / mapper]
  → [Undistortion] (if enabled)
  → [Brush Config JSON]
  → [Brush Training]
  → Output .ply file
  → [PlyCleaner] (optional PLY noise/floaters removal)
  → [Export to SPZ/GLB/OBJ/XYZ]
```

## i18n

9 languages via `assets/locales/{lang}.json`, **283 keys each**. `LanguageManager` singleton with Observer pattern. Fallback chain: selected → `en.json` → `fr.json` → empty dict.

Verrouillé par `tests/test_locales.py` : les 9 fichiers doivent avoir exactement les mêmes clés, aucune clé ne doit être orpheline (jamais référencée dans `app/`), et toute clé passée en littéral à `tr()` doit exister dans les 9 langues.

## Dual-Venv Setup

- **`.venv/`**: Main app (Python 3.13 recommended, PySide6, etc.)
- **`.venv_sharp/`**: ML Sharp (Python 3.11 — required by Apple's fork)
- **`.venv_360/`**: 360Extractor (isolated environment)

## CLI Subcommands

`pipeline`, `colmap`, `brush`, `sharp`, `view`, `upscale`, `4dgs`, `extract360`, `clean`, `splattransform`

Each has `--help`. No subcommand = GUI mode. Full reference: `CLI.md`

`pipeline` runs COLMAP → Brush, then Cleaning and Export when asked:
`--clean [light|medium|strong]` (bare flag = medium) and `--export FORMAT`
(`--export_output` to redirect). Checkpoints land in
`<output>/<project>/checkpoints`, the same place the interface uses.

## Journal — corruption historique réparée (2026-09-20)

Six enregistrements de `journal.jsonl` étaient structurellement invalides. **Cause trouvée** : le commit de ré-import massif `8e7af63` « Import CorbeauSplat v2 (Studio) » du 2026-08-08 — la version du 2026-07-19 (`cc95e9b`) est saine, celle d'après compte déjà les 6. Ce n'est donc pas le code d'écriture du journal qui est en cause, et la panne ne peut pas se reproduire d'elle-même.

**Dégât** : chaque ligne avait perdu une clé de tableau et son crochet ouvrant, laissant les éléments en chaînes nues dans l'objet. Rien ne plantait, le fichier étant lu ligne à ligne — d'où six semaines sans que personne le voie.

**Réparation** (contenu préservé au caractère près, vérifié par comparaison des multi-ensembles de chaînes avant/après) :
- L29 → `fichiers_modifies` — **certifiée** : la version pré-corruption contient le même enregistrement avec cette clé.
- L31 → `decisions` — **certifiée** : ordre des clés d'origine retrouvé dans `cc95e9b`.
- L26, L32, L35 → `decisions` — déduites de la position (entre `tests` et `graphify_rebuild`) et du contenu (phrases, pas des chemins).
- L30 → `features` — **et non `decisions`** : cette ligne portait déjà une clé `decisions` plus loin, et un doublon aurait été silencieusement écrasé par `json.loads`, perdant exactement le contenu récupéré. Sa valeur `session` avait en outre absorbé le libellé `lot` (`"10 UI 8 StudioWindow…"`) ; séparée en `session:"10"` + `lot:"UI 8 StudioWindow…"`, la concaténation redonnant la valeur fusionnée à l'identique.

**Garde-fou** : `tests/test_journal_integrity.py` — JSON valide ligne à ligne, absence de clés dupliquées, champs identifiants présents, dates ISO et ordre chronologique. Validé par réintroduction de la corruption d'origine : 3 des 4 tests tombent.

## ✅ Effet de bord corrigé — le chargement d'une configuration ne télécharge plus (2026-09-20)

`UpscalePanel._on_model_changed()` était branché sur `currentIndexChanged`, qui se déclenche aussi sur `setCurrentIndex()` — l'appel que fait `set_state()`. Recharger une configuration nommée désignant un modèle absent **lançait donc un transfert réseau** que l'utilisateur n'avait pas demandé ; `refresh_models()`, qui repeuple le combo, pouvait le déclencher aussi.

Corrigé en passant sur **`activated`**, qui ne se déclenche que sur un choix de l'utilisateur. Le téléchargement reste donc disponible, mais uniquement sur intention explicite.

La restauration ne se contente pas de se taire : `_refresh_model_status()` (branché sur `currentIndexChanged`, sans accès réseau) affiche « Modèle « X » non installé — sélectionnez-le pour le télécharger ». Sans ça, le silence aurait juste déplacé la surprise au moment où le traitement échoue. 1 clé i18n × 9 locales.

`tests/test_upscale_model_download.py` (6 tests, Qt réel en sous-processus) épingle les deux sens : construction du panneau, `set_state()` et `refresh_models()` ne téléchargent rien ; un choix utilisateur sur un modèle absent télécharge ; un choix sur un modèle installé ne télécharge pas. L'exclusion du combo modèle dans `test_gui_panel_state_roundtrip.py` est levée.

## RESTE À FAIRE (priorisé)

### ✅ Plan d'audit 2026-09-15 — **terminé le 2026-09-20**

Les 8 lots sont livrés. Lots 0/1/3 le 2026-09-16 (récupérés le 09-20 depuis des commits orphelins), Lot 2 résolu autrement le 09-17, Lots 4/5/6/7 le 09-20. Détail par lot ci-dessous.

### 🔴 Audit complet 2026-09-15 — `audit-report.md`
- Score global 6.5/10. Détail complet, corrections et plan d'action en 8 lots dans `audit-report.md` (destiné à un agent d'exécution sans contexte préalable, ex. Fable).
- **3 bloquants** : (1) les modes Sharp et 4DGS sont sélectionnables en GUI mais le bouton Lancer démarre toujours COLMAP (`app/gui/studio_window.py:438-442`) ; (2) export PLY ASCII cassé — `TypeError` reproduite (`app/core/export_engine.py:109`) ; (3) exports GLB/OBJ/XYZ silencieusement incolores, aucune conversion des harmoniques sphériques Brush (`f_dc_*`) vers RGB n'existe (`app/core/export_engine.py:130,162,239`).
- **16 importants** notables : déplacement en masse des `.ply` utilisateur au démarrage d'un training (`app/gui/workers.py:284-297`), upscale x1/x2 qui casse le nom du modèle (`app/core/upscale_engine.py:48-55`), `is_safe_path()` qui ne vérifie que l'existence du chemin malgré son nom (`app/core/base_engine.py:313-326`), 3 tests de sécurité tautologiques (`tests/integration/test_security_i18n_integration.py:36-56`), filtre anti-flou inaccessible en GUI (`app/gui/panels/reconstruction_panel.py`), dépôts tiers non épinglés (ml-sharp, nerfstudio, 360Extractor, supersplat).
- **13 mineurs** : 1 erreur ruff auto-corrigeable, 5 erreurs mypy neutralisables, `pytest-cov` absent (couverture non mesurée), 25 modules sans test.
- ✅ **Lot 0 fait (2026-09-16)** — M1 (ruff `--fix` sur `settings_window.py`), M2 (`AppKit.*`/`Foundation.*`/`spz.*` ajoutés aux overrides mypy → 0 erreur sur 78 fichiers), M3 (`pytest-cov` ajouté à `requirements-dev.txt` + extras dev + `DEP002`, artefacts de couverture gitignorés). **Couverture de départ : 39 %** (8598 instructions, 5209 non couvertes), 471 tests verts. Points bas : `upscale_engine` 16 %, `superplat_engine` 25 %, `export_engine` 37 %, panneaux GUI 9-19 %.
- ✅ **Lot 3 fait (2026-09-16)** — sécurité des données utilisateur. **I1** : le balayage des `.ply` au démarrage d'un training ne reconnaît plus que les checkpoints (`export_<n>.ply`, `iteration_<n>.ply`, `point_cloud/iteration_<n>/point_cloud.ply`, préfixés projet ou non) via `is_checkpoint_ply()`/`find_checkpoint_plys()` — archivage, préfixage projet, `handle_ply_rename` et surtout `_prune_to_latest_checkpoint` (qui *supprimait*) épargnent les fichiers de l'utilisateur et les archives antérieures ; le dossier de sauvegarde est créé **dans** `output_dir`, plus dans son parent ; chaque fichier déplacé est journalisé. **I13** : `clean_ply` écrit dans un temporaire puis `os.replace()` atomique (nettoyage sur place sans risque), conserve les éléments non-`vertex`, avertit si le fichier dépasse la RAM disponible, et honore un `cancel_check` — `CleanerWorker` le câble sur `isInterruptionRequested()` en mode fichier unique **et** par lots. **I14** : la boucle vidéo Sharp teste `stop_requested` en plus de `cancel_check`, n'écrit jamais dans un dossier préexistant (donc n'en supprime jamais un de l'utilisateur), affiche une estimation de durée et demande confirmation au-delà de 30 min via un aller-retour worker→GUI (`long_run_signal`/`answer_long_run`). i18n 4 clés × 9 locales. **485 tests** (+14), couverture 40 % ; `ply_cleaner` 79 %, `sharp_engine` 81 %.
- ✅ **Lot 1 fait (2026-09-16)** — exports fiables, tout dans `app/core/export_engine.py`. **C2** : l'export PLY ASCII fonctionne enfin — `text=True` est un argument du constructeur `PlyData`, pas de `write()` ; l'ancien appel levait un `TypeError` avalé par le `except`, donc l'option ne produisait jamais rien. **C3** : les couleurs des splats Brush/3DGS sont reconstruites depuis les harmoniques sphériques (`rgb = 0.5 + SH_C0 * f_dc_*`) par un helper unique `_extract_colors()` câblé sur XYZ, OBJ, GLB/trimesh et GLB/assimp — avant, tout export d'un résultat Brush sortait monochrome ; sans couleur du tout, l'export continue en géométrie seule avec un avertissement. **M5** : la ligne `opts.get('point_size')` qui n'assignait rien est supprimée (un GLB de nuage de points n'a pas cet attribut). **M6** : exporter un `.ply` vers son propre dossier est refusé au lieu de tenter `copy2` sur lui-même. **M7** : `assimp` est borné à `ASSIMP_TIMEOUT_SECONDS = 300` s, sinon l'export ne pouvait plus être interrompu. **M13** : les commentaires du fichier sont passés en anglais (0 restant). 11 tests ajoutés (PLY synthétique avec `f_dc_*`) : **497 tests verts (+11)**, ruff/mypy/deptry propres, couverture du fichier 37 % → 55 %.
- ✅ **Lot 4 fait (2026-09-20)** — honnêteté des contrats : chaque nom et chaque docstring décrit désormais ce que le code fait vraiment. **I4** : `is_safe_path()` renommée `path_exists()` — elle promettait un confinement jamais implémenté, alors qu'aucun appelant applicatif ne l'utilisait (seulement des tests) ; le paramètre mort `project_root` de `validate_path_standalone()` est supprimé. Choix assumé : **pas de confinement**, l'utilisateur choisit légitimement ses dossiers n'importe où (Bureau, volume externe). **I5** : les 3 tests de sécurité tautologiques sont réécrits — ils passaient uniquement parce que les chemins testés n'existaient pas, et l'un finissait par `assert result is None or isinstance(result, Path)`, vrai quoi qu'il arrive ; ils vérifient maintenant la normalisation réelle des `..` sur une cible **existante**. **I6** : la docstring de `delete_project_content()` annonçait une liste blanche (« only if contained within project_root or user home ») là où le corps est une liste noire ; corrigée, avec un test qui épingle qu'un dossier hors des deux racines **est** supprimable (projets sur disque externe). **I11** : `ExportEngine.is_available()` ne retourne plus `True` en dur — nouvelle `missing_dependency(format)` qui sonde réellement plyfile/spz/trimesh-open3d-assimp, appelée en tête d'`export()` pour échouer avant de travailler ; `SharpEngine.is_installed()` est redéfinie comme « `_get_sharp_cmd()` a trouvé de quoi lancer », supprimant la divergence entre les deux et le repli `[sys.executable, "-m", "sharp.cli"]` qui lançait le Python 3.13 du venv principal alors que Sharp exige 3.11 ; `check_dependencies()` couvre enfin brush/glomap/upscayl-bin/npx, chacun étiqueté par la fonction qu'il bloque, et **la GUI l'exécute enfin** (journalisée au démarrage via `_report_missing_dependencies()`, pas de dialogue modal — la plupart des entrées sont par fonctionnalité). 14 tests ajoutés : **585 tests verts (+14)**, ruff/mypy propres.
- ✅ **Lot 5 fait (2026-09-20)** — convergence CLI/GUI : les deux interfaces font enfin la même chose. **I7** était déjà résolu depuis l'audit — le filtre de flou vit dans le panneau **Source** (`chk_filter_blur`/`combo_blur`), relié au moteur par `apply_source_blur_settings()` et testé ; le constat visait `reconstruction_panel.py`, où il n'a jamais été. **I8** : `pipeline` s'arrêtait après Brush malgré son nom ; il enchaîne désormais Nettoyage et Export via `--clean [light|medium|strong]` (drapeau nu = medium) et `--export FORMAT`, avec numérotation d'étapes dynamique (« Étape 3/4 » au lieu de « 1/2 » mensonger). **I9** : la CLI passait `dataset_path` comme sortie de Brush là où la GUI applique `<sortie>/<projet>/checkpoints` — un projet lancé d'un côté et repris de l'autre perdait ses checkpoints. La CLI appelle maintenant `resolve_checkpoints_dir()`, source unique de cette sémantique. Au passage, `is_checkpoint_ply()`/`find_checkpoint_plys()` migrent de `workers.py` (Qt) vers `chaining_logic.py` (sans Qt) pour que les deux interfaces partagent la même règle ; réexportées depuis `workers.py` pour les appelants existants. **I10** : `--sequential_overlap` et `--guided_matching` n'étaient pas déclarés alors que `_build_colmap_params` les lisait par `getattr` — silencieusement figés sur leur défaut, et `guided_matching` était exposé en GUI mais inatteignable en CLI ; les deux drapeaux « défaut à True » (`--view-graph-calibration`, `--ignore-watermarks`) passent à `BooleanOptionalAction`, leur forme positive étant un no-op. **M9** : `manifest.md` documentait 9 sous-commandes sur 10 (`splattransform` oubliée). Deux garde-fous ajoutés contre le retour de la dérive : un test paramétrique **ColmapParams ↔ drapeaux CLI** (25 champs, zéro exception) et un test qui épingle `manifest.md` sur `DISPATCH`. 21 tests ajoutés : **597 tests verts (+21)**, ruff/mypy propres.
- ✅ **Lot 6 fait (2026-09-20)** — chaîne d'approvisionnement. **I16** (le vrai risque) : `ml-sharp`, `nerfstudio` et `360Extractor` étaient clonés puis `reset --hard` sur leur branche par défaut, donc **compilés et exécutés** dans la version poussée ce jour-là ; `EngineDependency` accepte désormais un `pinned_ref` que `update_git()` sort en `checkout`, et les trois sont épinglés (nerfstudio `v1.1.5`, 360Extractor `v4.0.1`, ml-sharp sur le commit `aed6527` — ce dépôt ne publie aucun tag). **I15** : `get_brush_build_mode()` retombait sur `release` dès que `brush.version` manquait, et un build source recevait alors `--total-steps` qu'il rejette, tuant l'entraînement aussitôt ; la détection interroge maintenant `brush --help` (résultat mis en cache) avant de se rabattre sur un défaut. Plus le `TypeError` sur `checkpoint_interval=None`. **I2** : l'heuristique qui réécrivait `realesrgan-x4plus` en `realesrgan-2plus` (modèle inexistant) est supprimée — l'échelle part telle quelle à `upscayl-bin` via `-s` ; CLI et GUI proposent enfin le même ensemble 1-4, `x1` étant conservé car il sert à convertir sans agrandir. **I3** : catalogue épinglé sur un commit au lieu de `main`, entrée Real-ESRGAN dupliquée fusionnée (avec alias pour les configs existantes), **les 10 empreintes SHA256 complétées et vérifiées contre le commit épinglé**, vérification passée en **fail-closed** (un modèle sans empreinte était installé sans contrôle), et téléchargement en streaming au lieu de garder ~70 Mo deux fois en mémoire. **I12** : l'origine CORS de repli nommait le port du serveur de données au lieu de celui du viewer ; le socket est désormais lié dans le thread appelant, supprimant la fenêtre où `stop_data_server()` lisait `None` et laissait un serveur tourner sans référence ; préconditions `dist/` et `npx` vérifiées avant lancement. **M10** : `AGENTS.md` et `manifest.md` annonçaient PyQt6 alors que **zéro fichier** ne l'utilise (tout est PySide6) ; `requires-python` borné à `<3.14` pour refléter le lanceur. **M12** : `ARG001` activée — les paramètres morts de fonctions libres, exactement le cas manqué au Lot 4 ; `ARG002`/`ARG005` restent désactivées (interfaces uniformes) et les tests sont exemptés (arguments de `@patch`). **M8 non traité** : `linux_brush` vide dans `checksums.json`, en attente d'accord explicite (sans effet réel, projet macOS). 24 tests ajoutés dont `tests/test_superplat_engine.py` (nouveau) : **616 tests verts (+19)**, ruff/mypy propres.
- ✅ **Lot 7 fait (2026-09-20)** — dette de fond. **M11** : `BrushWorker.run()` passe de **153 à 36 lignes** — elle mêlait résolution du dataset, montage de l'environnement Refine, archivage des checkpoints, lancement du process et traitement du résultat dans un seul corps à l'intérieur d'un `QThread`. Découpée en `_resolve_dataset()`, `_prepare_refine_environment()`, `_find_latest_checkpoint()`, `_set_refine_start_iteration()`, `_archive_existing_checkpoints()`, `_train()` et `_finalise()` ; comportement d'erreur préservé à l'identique (les étapes faillibles émettent `finished_signal` puis renvoient `None`). **M4** : le chemin Refine, jusque-là enfoui et donc non couvert, gagne 8 tests (repli sans checkpoint, reprise d'itération depuis le nom du fichier, échec de copie, repli symlink→copie) et `_finalise()` 2 tests dont « un entraînement échoué ne renomme ni n'élague les fichiers utilisateur » ; nouveau `tests/test_upscale_engine.py` (14 tests) porte `upscale_engine.py` de **17 % à 77 %** — la logique de tile adaptatif méritait d'être épinglée, un mauvais tile faisant planter upscayl-bin en SIGBUS sur mémoire unifiée. `workers.py` 41 % → 52 %, couverture globale **39 % → 44 %**. **M13** : conversion FR→EN terminée, 25 fichiers de tests traités, **zéro occurrence restante** dans tout le dépôt. 24 tests ajoutés : **638 tests verts (+22)**, ruff/mypy propres.
- Socle sain confirmé : aucun cycle d'import, aucun `shell=True`/`eval`/`exec`, 471 tests verts. Couche Apple Silicon jugée satisfaisante (nice priority, WGPU Metal, VideoToolbox, détection Rosetta) — réserve levée le 2026-09-20 : l'alerte Rosetta passait par `warnings.warn` (stderr), donc invisible au lancement par l'icône — exactement l'utilisateur qu'elle vise. Remplacée par `rosetta_warning()`, qui **retourne** le texte au lieu de l'émettre, chaque interface le plaçant où ses utilisateurs regardent : la CLI l'imprime, la GUI le met **en première ligne du journal** (sous Rosetta, toutes les durées qui suivent sont 20-40 % pires que ce que la machine peut faire). Pas de dialogue : l'application fonctionne, elle est seulement plus lente.

### 🌍 Code Localization — Comments to English
- ✅ **Terminé au 2026-09-20** — plus aucun commentaire ni docstring en français dans `app/` **ni** dans `tests/` (vérifié par balayage). Le décompte historique de « ~76 commentaires » était très sous-évalué : l'audit du 2026-09-19 en relevait 598 dans 66 fichiers.
- **From now on**: ALL new comments MUST be in English (GUI, core, tests, CLI). Rule applies retroactively to existing code when touched.

### 🎞️ Formats d'entrée (2026-09-19)
- ✅ **Aller-retour de configuration vérifié sous vrai Qt (2026-09-20)** — le mock PySide6 de `conftest.py` est installé pour toute la session et irréversible, donc aucun panneau ne peut être instancié réellement ailleurs ; les contrats AST prouvent qu'un combo est peuplé et lu, pas que sa valeur survit à une sauvegarde. Nouveau `tests/test_gui_config_roundtrip.py` : un **sous-processus unique** en PySide6 réel offscreen joue tous les scénarios (défaut, écriture disque, relecture en fenêtre neuve, propagation jusqu'à `ColmapParams`, valeur `off`, config antérieure sans la clé) et les 6 tests analysent sa sortie — un lancement coûte ~1 s, quatre auraient doublé la durée de la suite. Validé par sabotage : en retirant la clé de `get_state()`, les tests échouent bien, avec des messages explicites (`assert 'png' == 'jpeg'`) et non une erreur de fixture. Suite : 3 s → 6,5 s.
- ✅ **Widget de conversion d'images dans le panneau Source (2026-09-20)** — combo `png`/`jpeg`/`off` sous le filtre de flou, avec infobulle expliquant que les originaux ne sont jamais modifiés et que `off` fait refuser les fichiers par COLMAP plus loin. Le réglage existait en CLI (`--convert`) depuis la session du 19 mais restait inatteignable en GUI, figé sur `png` — dernière divergence CLI/GUI connue. Le pont `apply_source_blur_settings()` est **renommé `apply_source_settings()`** : il ne portait plus seulement le flou (même raisonnement qu'au Lot 4, un nom doit décrire ce que le code fait). Sérialisé automatiquement dans `ChainConfig.source`, qui recopie `get_state()` ; les configs antérieures n'ont pas la clé et retombent sur `png`, comportement historique. 5 clés i18n × 9 locales, clés **littérales** et non construites par f-string — `test_no_orphan_locale_keys` analyse les appels `tr()` statiquement et l'a signalé. 5 tests.
- ✅ **Réglage de conversion exposé dans le message d'erreur (2026-09-20)** — une source au format inconnu (RAW appareil photo, PSD) faisait échouer le lancement sur « Chemins manquants », alors que le chemin était valide : `detect_source_kind()` réduit **tout** échec à `"empty"`. Nouveau `describe_unusable_source()` (dans `reconstruction_logic.py`, sans Qt) qui distingue chemin absent / fichier au format non pris en charge / dossier sans média, **nomme les formats réellement trouvés**, liste les formats acceptés depuis les jeux partagés de `media.py` (pas une seconde copie à maintenir), et surtout expose le réglage de conversion — c'est lui qui rend la liste plus large que JPEG/PNG. Avec `convert=off`, le message avertit que rien ne sera converti et que la chaîne refusera ces fichiers plus loin. La GUI n'ayant toujours pas de widget pour ce réglage, ce message est la façon dont un utilisateur non-CLI apprend qu'il existe. 8 tests.
- **Vérifier le câblage conversion d'images avec l'Upscale quand l'étape est demandée** — la conversion (HEIC/TIFF/BMP/WebP… → PNG) a été validée hors GUI : ingestion COLMAP, `run_upscale_job` en x4 et en x1 sur un dossier mixte (jpg/tif/bmp/webp/heic), 5/5 images produites. Reste à contrôler le parcours réel depuis l'interface, drapeau `upscaler_avant` coché, source non standard : chaîne Source → Upscale → Reconstruction, et le bouton Lancer local du panneau Upscale.
- ✅ **Fenêtre de sélection vidéo (point in / point out) — faite le 2026-09-20.** Bouton « Sélection vidéo… » dans le panneau Source, **actif uniquement pour une vidéo seule**, grisé pour un dossier (plusieurs vidéos = plusieurs chronologies, qu'une seule paire entrée/sortie ne peut décrire) avec infobulle explicative. **Aperçu par images fixes FFmpeg et non QtMultimedia** : ffmpeg est déjà une dépendance dure et décode les 28 conteneurs de `VIDEO_EXTENSIONS`, là où le backend Qt échoue silencieusement sur `.mts`, `.insv`, `.mxf` ; le coût est qu'on parcourt des images fixes au lieu de lire la vidéo — acceptable, le but étant de choisir deux instants. `-ss` avant `-i` (seek par image clé, donc instantané même sur une longue vidéo), anti-rebond de 120 ms au scrub. **Chaîne complète** : `VideoRangeDialog` → `SourcePanel.get_state()["video_trim"]` → `ChainConfig.source` → `apply_source_settings()` → `ColmapParams.video_trim_start/end` → `-ss`/`-t` dans `extract_frames_from_video()`. La durée est passée en **`-t` et non `-to`** : après un `-ss` pré-entrée, ffmpeg compte `-to` depuis le point de seek et couperait silencieusement la mauvaise plage. Une sélection couvrant toute la vidéo est enregistrée comme `None`, pour qu'aucun trim inutile ne traîne dans la config ni dans la commande ; une plage inversée est ignorée avec avertissement (ffmpeg accepte un `-t` négatif et ne produit rien, ce qui ressemble à une extraction cassée plutôt qu'à un réglage fautif). Le trim est oublié si la vidéo source change. Parité CLI : `--trim_start`/`--trim_end` (imposée par le test paramétrique du Lot 5, qui a échoué tant que les drapeaux manquaient). 13 clés i18n × 9 locales. **Saisie manuelle** : deux champs de timecode à côté des boutons, `parse_timecode()` étant l'inverse exact de `format_timecode()` (aller-retour vérifié) — accepte `90`, `1:30`, `00:06.0`, `1:02:05.2`, virgule décimale ; une saisie illisible laisse le repère intact et le champ est réécrit depuis lui, pour qu'il n'affiche jamais autre chose que la plage réelle ; une sortie antérieure à l'entrée est refusée, une valeur au-delà de la durée est bornée. **Conseil de format** : hors `.mp4`/`.mov`, la fenêtre signale que le positionnement peut manquer de précision (disposition des images clés des formats broadcast/action-cam) et suggère la conversion — la découpe fonctionne quand même, c'est un conseil, pas un blocage. **57 tests** dont l'extraction réelle sur un clip généré : 12 s → 60 images, plage 3→9 s → 30 images.

### ✅ Tranché — bouton Lancer local onglet Entraînement (2026-09-20)
- **Décision : retiré.** `EntrainementPanel` sert à deux endroits ; le bouton n'existe plus que dans l'instance `standalone=True` (module OUTILS « Brush »), où l'utilisateur saisit lui-même les chemins d'entrée et de sortie.
- **Pourquoi** : l'onglet Entraînement n'a pas de chemins manuels, le bouton devait donc les déduire. Une première version les a mal déduits et a déplacé le dossier racine d'un projet utilisateur via la logique d'archivage de checkpoints (`shutil.move` vers `checkpoints_backup_<timestamp>` ; contenu retrouvé intact).
- L'entraînement dans la chaîne reste lancé par le bouton unique de Source, ou automatiquement après la Reconstruction si « Lancer Brush » est coché (`_on_reconstruction_standalone_finished`).
- Risque résiduel nul depuis le Lot 3 récupéré : `is_checkpoint_ply()` ne reconnaît que les checkpoints, les fichiers de l'utilisateur ne sont jamais déplacés.

### Autres tâches

1. **Validation manuelle utilisateur sur données réelles** ✅ — confirmé par l'utilisateur sur données réelles (clic-par-clic dans `StudioWindow`)** — les moteurs sous-jacents sont désormais vérifiés par tests e2e réels (COLMAP→Brush→Nettoyage→Export SPZ, Sharp image/vidéo, Upscale : 20/20, vrais binaires) et le bug qui empêchait tout lancement d'entraînement depuis l'UI est corrigé (session 2026-07-19). Reste : passage utilisateur réel dans l'interface avec ses propres fichiers/3 pipelines/7 modules OUTILS, sur Apple Silicon — aucun outil automatisé ne remplace ce contrôle.
2. **E2E 360 Extractor** ✅ — session 12, 4 tests (mode image, `layout: ring`), marqueur `e2e_360` (`.venv_360` requis, générateur équirectangulaire à créer ; un script factice généré en session 2026-07-19 a été retiré car il ne validait rien de réel)
3. **E2E 4DGS Phase A** ✅ — session 12, 4 tests (mode COLMAP dégradé), marqueur `e2e_4dgs`. Phase B (nerfstudio, `.venv_4dgs`) restante. (ffmpeg + COLMAP + nerfstudio, coûteux)
4. **Audit 2026-08-07 — arbitrages tous tranchés** ✅ (détail et justification dans `audit-report-2026-08-07.md`). Les findings Critique (`A2-C1`) et Important (`A2-I1`→`A2-I10`) étaient déjà corrigés ; les 4 points renvoyés à l'utilisateur sont clos (historique ci-dessous). **Ne reste de cet audit que les vérifications manuelles sur Mac** — cf. points 6 et 7 de cette liste, et la section « À vérifier manuellement sur Mac » du rapport.
   - **A2-M3** ✅ — résolu (2026-09-11, commit `9746c09`) : clés orphelines `darwin_glomap`/`linux_glomap` retirées de `app/scripts/checksums.json`, dans le cadre de l'audit dépendances tierces demandé par l'utilisateur (« corrige d'abord »), qui a explicitement autorisé la modification de ce fichier normalement hors périmètre autonome (AGENTS.md).
   - **A2-M8** ✅ résolu (constaté 2026-09-17) : les trois fonctions signalées « testées, sans appelant applicatif » ont toutes été câblées depuis — `delete_user_preset()` par le bouton 🗑 du dropdown de presets (`app/gui/panels/entrainement_panel.py:308`), `delete_config()` par le bouton « Supprimer… » du bloc « Paramètres actuels » (`app/gui/studio_window.py:1191`, via `delete_config_dialog`), `download_model_files()` par le téléchargement threadé de modèles Upscale (`app/gui/widgets/upscale_widgets.py:31`). Aucun arbitrage restant : rien à supprimer, rien à câbler.
   - **A2-M14** ✅ — tranché (2026-08-07) : `requests` et `urllib3` retirés de `requirements.txt`, `requirements.lock` et `pyproject.toml`. Aucun fichier ne les importait (tout le réseau passe par `urllib.request`, stdlib) et, dans l'ensemble runtime, rien ne les tirait : `pip show` les donne requis uniquement par `CacheControl`/`pip-audit`, des outils de dev. Ils restent donc installés en dev, jamais chez l'utilisateur final.
   - **A2-I5** ✅ — tranché : l'automatisation **était** souhaitée. L'enchaînement nettoyage+export+visualiser après entraînement est rebranché, non pas via un `PostTrainingWorker` séparé mais dans le walk `_run_pipeline_step` existant (session 14).
5. ✅ **Résolu (2026-09-17)** — le sélecteur de mode route réellement les 3 modes. `plan_pipeline()` façonnait déjà le plan par mode, mais l'étape `reconstruction` construisait toujours un `ColmapWorker` : choisir Sharp ou 4DGS puis « Lancer » exécutait COLMAP quand même. `StudioWindow._build_reconstruction_worker()` aiguille maintenant sur le mode retenu au lancement (gsplat → COLMAP, sharp → `SharpWorker`, 4dgs → `FourDGSWorker`) ; Sharp et 4DGS prennent leurs chemins du panneau Source, leurs réglages de leur propre panneau. 4DGS s'arrête toujours à la préparation de dataset — aucun entraîneur 4DGS natif Apple Silicon n'existe. 4 tests (`tests/integration/test_tool_launch_integration.py`).
6. **Validation manuelle sur Mac de la refonte session 14** — aucun test automatisé ne couvre le rendu Qt réel. À vérifier : FPS visible seulement en source vidéo, bannière de mélange images+vidéo, colonne droite et filet masqués sur les bons écrans, chaînage réel bout-en-bout sur vraies données, molette sur les combos Réglages (ne doit plus rien changer), bannière 4DGS et tooltip `4dgs` du combo Mode. **Ajouts du 2026-09-17 à vérifier aussi** : galerie de modèles Upscale (volet droit — poids affiché, Télécharger puis Supprimer, modèles « Inclus » sans action, combo et cartes qui restent d'accord) ; réglages COLMAP du panneau 4DGS (chevauchement grisé hors matcher séquentiel) ; « Lancer » en mode Sharp puis en mode 4DGS depuis le panneau Source.
7. **Validation manuelle sur Mac des chantiers du 2026-08-07** (Upscale, champs Projet, suppressions, 360) — la GUI PySide6 n'a pas été exécutée pendant le chantier. À vérifier : Upscale en tête du groupe PARAMÈTRES (filet vertical continu l'incluant) et absent d'OUTILS ; colonne droite et filet masqués sur la page Upscale ; toggle « Upscaler avant reconstruction » en tête du bloc Automatisation, décoché au démarrage ; **chaîne source images** — fichiers présents dans `<sortie>/<projet>/images_upscaled` *avant* le démarrage de COLMAP et dossier source d'origine intact ; **chaîne source vidéo** — message différé affiché puis `images_src/` créé pendant la Reconstruction ; bouton Lancer local depuis PARAMÈTRES › Upscale, indépendant du Lancer global ; sauvegarde/rechargement d'une configuration nommée avec réglages Upscale non-défaut. **Extraction 360** — en tête de PARAMÈTRES (avant Upscale) tout en restant dans OUTILS, les deux pages devant être indépendantes (saisir un chemin dans l'une ne change rien dans l'autre) ; chaîne `source_360` seul puis `source_360` + `upscaler_avant`, avec `images_360/` peuplé *avant* `images_upscaled/`, lui-même *avant* le démarrage de COLMAP. **Champs Projet** — `checkpoint_dest` renseigné : l'export chaîné doit retrouver le PLY dans `<destination>/<projet>/` (c'est le cas de régression réel du chantier) ; `export_dir`/`export_format` renseignés : le run chaîné doit les respecter plutôt que la saisie du panneau Export. **Suppressions** — 🗑 grisé sur « Défaut » et sur un preset intégré, actif sur un preset personnel ; bouton « Supprimer… » des Réglages (liste, confirmation, disparition effective).
8. ✅ **Résolu (2026-08-07)** — les 4 signalements deptry sont traités et l'outil est câblé dans le job `lint` de la CI, donc ils ne peuvent plus s'accumuler en silence. `requests`/`urllib3` retirés (cf. A2-M14 ci-dessus) ; `spz` ignoré sous `DEP003` en plus de `DEP001`, avec `open3d` et `bpy` — les deux listes couvrent le même ensemble, pour que le verdict ne dépende pas de l'état du venv. Vérifié dans les deux conditions : venv local peuplé, et environnement isolé reproduisant la CI. _Historique du signalement initial :_ l'outil était configuré dans `pyproject.toml` mais absent de `requirements-dev.txt`, de l'extra `dev` et de la CI, donc jamais exécutable (2026-08-07 — il était configuré dans `pyproject.toml` mais absent de `requirements-dev.txt`, de l'extra `dev` et de la CI, donc jamais exécutable). Aucun n'a été corrigé, chacun demande un arbitrage : **DEP003 ×2** — `spz` importé (`export_engine.py:344`, `installers/spz.py:35`) alors qu'il n'est qu'une dépendance transitive ; il est compilé et installé dans le venv principal par son propre installeur, donc jamais déclaré. Soit le déclarer, soit l'ignorer explicitement comme `open3d`/`bpy`. **DEP002 ×2** — `requests` et `urllib3` sont déclarés mais **aucun fichier ne les importe** : tout le réseau passe par `urllib.request` (stdlib, à ne pas confondre avec `urllib3`). Vérifié par grep. Ce sont probablement deux dépendances mortes, mais les retirer change l'empreinte d'installation. À trancher, puis **câbler deptry dans la CI** pour que ces écarts ne repartent pas silencieusement.
9. **Décisions en attente de l'utilisateur** (2026-08-07) : **(a)** ✅ tranché (2026-09-17) — `CollapseState` **supprimée** de `app/gui/studio_nav.py` avec ses 3 tests : `logbar.py`, son unique consommateur applicatif, avait disparu à la refonte, et aucun autre appelant n'a pris le relais (le graphe graphify annonçait un import depuis `rail.py`, arête périmée démentie par le code). 468 tests pass. **(b)** ✅ tranché (2026-09-17) — le journal ne doit **jamais** s'ouvrir tout seul, mais une erreur doit **proposer** de le consulter. La boîte de dialogue d'échec porte désormais un bouton « Voir le journal » à côté de OK (`StudioWindow._show_error_dialog()`, utilisé par `_on_worker_finished` et `_fail_pipeline_step`) ; l'ouverture reste un geste de l'utilisateur. Le clic sur l'étape ⛔ du rail reste un second accès. Nouvelle clé i18n `btn_view_logs` × 9 locales. **(c)** ✅ tranché (2026-09-17) — la voie `BaseWorker.parse_line` est **abandonnée** : Brush n'émet pas sa progression, et la faire émettre supposerait de modifier Brush lui-même, ce qui est hors de question. Remplacée par une estimation depuis le disque : `BrushWorker._start_checkpoint_progress()` compte les `.ply` exportés et les rapporte à `total_steps / checkpoint_interval`. **Volontairement grossier** — un cran par intervalle `--export-every` (4 crans pour 30 000 pas à 7 000), rien avant le premier export ; c'est un témoin d'avancement, pas une barre fine. Le dossier surveillé est lu au lancement, une fois `output_path` figé par la redirection mode Refine et l'archivage ; les `.ply` déjà présents sont exclus pour qu'une reprise ne démarre pas à 100 %. Désactivé si `checkpoint_interval` vaut 0 ou si `total_steps` est absent. 3 tests (`tests/test_workers.py`).
10. **L'épinglage Brush devra être bumpé à la main** à chaque release amont (`brush_release` + empreinte, ensemble). C'est désormais **signalé au démarrage** au lieu de casser l'installation, mais ça reste une action manuelle récurrente. Automatiser suppose une racine de confiance indépendante de l'artefact (attestations GitHub/sigstore) — pas le sidecar `.sha256`, qui vient de la même origine.
11. ✅ **Résolu (2026-09-17)** — la galerie `ModelCard` est restaurée, dans le volet droit du panneau Upscale (jusque-là un `QWidget` vide). Une carte par modèle : nom, description, badge d'échelle, état (✅ + poids disque / Non installé / Inclus) et bouton Télécharger ou Supprimer. Les cartes n'ont pas de worker propre — elles émettent, le panneau télécharge — et partagent le garde `_dl_worker` du combo, donc un clic sur carte et un changement de combo ne peuvent pas lancer deux transferts. `UpscaylModel.size_on_disk_mb()` rétablie (3 tests). 8 clés i18n × 9 locales.
12. ✅ **Résolu (2026-09-17)** — le panneau 4DGS expose `camera_model`/`single_camera`/`matcher_type`/`sequential_overlap` (clés i18n réutilisées du panneau Reconstruction), `_launch_fourdgs()` et le chemin chaîné construisent `colmap_params`. Le champ chevauchement se grise hors matcher séquentiel, et la liste des matchers est limitée aux deux modes que `run_colmap()` implémente réellement — `vocab_tree`, proposé par le panneau Reconstruction, y retomberait silencieusement sur `exhaustive`.
13.5. ✅ **Résolu (2026-09-17)** — le chemin `ns-process-data` souffrait bien du même problème : `process_dataset()` lui passait tout `images/`, or `ns-process-data` lance COLMAP en interne — donc SfM sur scène non statique, exactement le bug corrigé dans `run_colmap()`. En prime il attend un dossier plat et ne serait pas descendu dans les sous-dossiers `cam_XX`. Les deux branches consomment désormais le même jeu de référence produit par `_build_static_reference_set()`.

14. **360Extractor `--export-colmap`** (2026-09-11) — flag découvert lors de l'audit dépendances tierces, non intégré. Écrit une calibration virtuelle + config de rig + script `reconstruct.py` en plus des images extraites, exploitables par COLMAP (`rig_configurator`) pour éviter à la Reconstruction de redeviner des poses caméra déjà connues (utile en 360°, layout `cube`). **Non implémenté** : upstream indique lui-même « full reconstruction qualification is pending » — feature encore expérimentale côté 360Extractor. Design d'intégration esquissé si repris : flag `RunState.extraction360_export_colmap` (opt-in, actif seulement si layout `cube`), `Extractor360Engine` ajoute `--export-colmap`, `_build_colmap_worker()` bascule vers un chemin de reconstruction assisté (logique répliquée dans `colmap_commands.py`, pas d'appel au script externe généré, pour rester cohérent avec les garanties sécurité du reste du code) avec repli automatique sur la Reconstruction standard en cas d'échec/COLMAP trop ancien. Pas de changement d'ordre du pipeline nécessaire (Extraction 360 tourne déjà avant Reconstruction). À reprendre seulement après validation de la qualité de sortie côté 360Extractor.

13. ✅ **Résolu (2026-09-17)** — `SUPERSPLAT_PINNED_TAG` (`v3.3.0`) épingle la release installée et sert de version distante pour la comparaison ; un tag amont plus récent est seulement signalé, jamais adopté tout seul. Même raisonnement que l'épinglage Brush, sans `checksums.json` : SuperSplat est construit depuis les sources, il n'y a pas d'empreinte d'archive à garder en phase. Bumper le tag reste un geste relu.

15. **LightGlue/ALIKED déjà actif, LoMa (COLMAP 4.2.0) à évaluer** (2026-09-13) — Audit ONNX : `SIFT_LIGHTGLUE`/`ALIKED_LIGHTGLUE` sont déjà entièrement câblés (`app/core/params.py`, `app/core/colmap_commands.py`, CLI `app/cli/parser.py`, GUI `reconstruction_panel.py`) et accélérés par défaut sur Mac via CoreML (`COLMAP_COREML_ENABLED` actif inconditionnellement sur macOS dans le build COLMAP, `use_gpu=1` non surchargé par l'app) — **à vérifier en pratique** avant de considérer le gain acquis : logs `GLOG_v=2` doivent afficher « Enabling CoreML execution provider », sinon repli silencieux vers le CPU. Piste ouverte : matcher **LoMa** (`LOMA_B/B128/L/G/R`, PR colmap/colmap#4524, mergé en 4.2.0 — version déjà installée) annoncé plus robuste que LightGlue sur scènes difficiles (faible texture, gros mouvements caméra), mais poids encore hébergés sur le repo perso de l'auteur (pas encore asset officiel COLMAP). **Non implémenté** — à ajouter seulement si un test réel (comparaison sur nos propres jeux d'images) montre LightGlue insuffisant ; extension mineure des mêmes structures, aucune nouvelle dépendance.

> Résolu : **4DGS mode dégradé — COLMAP tournait sur toutes les frames de toutes les caméras** — 2026-09-10. Bug signalé par l'utilisateur (« l'entraînement colmap pour 4DGS n'est pas le bon »), confirmé par comparaison avec le pipeline référence `hustvl/4DGaussians` (`multipleviewprogress.sh` + `scripts/extractimages.py`, qui ne copient que `frame_00001` par caméra avant COLMAP). `FourDGSEngine.run_colmap()` passait tout `images/` (sous-dossiers `cam_XX` avec toutes les frames extraites, tous timesteps confondus) à `feature_extractor`/`mapper` — viole la prémisse de scène statique du SfM : matcher des frames prises à des instants différents d'une scène qui bouge produit des correspondances invalides (reconstruction corrompue). Fix : nouvelle méthode `_build_static_reference_set()`, sélectionne 1 frame (la première, ordre alpha) par sous-dossier `cam_XX`, copiée dans `<dataset_root>/colmap_reference_frames/` — c'est sur ce jeu que tourne COLMAP désormais. Dossiers `cam_XX_src` (résidus d'upscale) explicitement exclus du scan. Fallback conservé : sans sous-dossiers caméra (dataset mono-caméra à plat), comportement inchangé. 2 tests ajoutés (`tests/test_four_dgs_engine.py`). 463 tests pass, 1 skip. Le chemin `ns-process-data` (nerfstudio), resté ouvert ici, a été corrigé de la même façon le 2026-09-17 — cf. point 13.5.

> Résolu : **Réglages Load/Save/Delete/Reset déménagés vers le panneau Projet** — 2026-09-09. La fenêtre Réglages généraux (`settings_window.py`) ne porte plus que Thème/Langue/Notifications ; un pied de page centré (nom + version de l'app, lien « Voir le changelog » ouvrant `CHANGELOG.md` via `QDesktopServices`) a été ajouté, avec un espacement renforcé sous la case Notifications (ajustement demandé après premier retour). Les actions Charger/Sauvegarder/Supprimer/Réinitialiser une configuration — des actions de workflow projet, pas des préférences globales — vivent désormais dans un bloc « Paramètres actuels » du panneau Projet (`source_panel.py`), sous Automatisation. Nouvelle clé i18n `settings_changelog_link` (9 locales). 460 tests pass, 2 skip.

> Résolu : **journal sorti de la fenêtre principale, barre d'activité, rail réordonné** — 2026-08-07. `app/gui/logbar.py` **supprimé** au profit de `activity_bar.py` (`ActivityBar` : étape en cours, détail, progression, Annuler) et `logs_window.py` (`LogsWindow`, non modale, encapsulant le `LogsTab` existant — recherche/copie/sauvegarde déjà en place). Renommage assumé : un composant nommé « barre de logs » n'affichant aucun log aurait piégé la prochaine lecture. La fenêtre de journal est **alimentée même fermée**, pour que l'historique complet du run soit présent à la première ouverture. Bouton `▤ Logs` à côté de `⚙ Paramètres`, sans nouvelle clé i18n. **Deux régressions de la refonte UI corrigées au passage** : `progress_signal` était émis par 4 workers (COLMAP, 360, Sharp vidéo, Export) mais **connecté nulle part** — la progression était calculée puis jetée ; et le journal s'ouvrait de force à chaque lancement de worker. L'activité est branchée sur `log_signal` **et** `status_signal` car `BrushWorker` n'émet ni progression ni statut (son moteur ne reçoit qu'un `logger_callback`) — s'en tenir au statut aurait laissé l'entraînement muet. Nouveau `rail.item_label()` pour que la barre nomme l'étape avec exactement le libellé du rail. Rail également réordonné (ordre de lecture ≠ ordre d'exécution, écart documenté) et **14 icônes désormais uniques** (`⌂` pour Projet, `⊘` pour Nettoyage, `⬡` pour l'outil 360). Barre du bas : marges `(8,2,8,2)` → `(10,6,10,8)`, les boutons touchaient le bord de la fenêtre (mesuré : 899 px sur 900). Toutes les vérifications faites par instanciation offscreen réelle, pas par lecture de code. 456 tests pass, 1 skip.

> Résolu : **démarrage — installation de Brush débloquée + 3 correctifs de lanceur** — 2026-08-07, suite au reset complet de l'utilisateur. **(1)** Brush ne s'installait plus : `checksums.json` épinglait `darwin_brush` sur une empreinte de release antérieure, et la garde fail-closed `verify_download_strict()` (audit A2-C1) refusait donc l'installation de v0.3.0 — le message d'erreur orientait à tort vers la connexion réseau. Repointé sur `65b26313…a1048c`, recoupé auprès de **trois sources indépendantes** (champ `digest` de l'API GitHub, sidecar amont `<asset>.sha256`, manifeste amont `sha256.sum`) ; `updated_at == published_at` sur l'asset confirme un pin périmé et non un artefact réuploadé. Le hash n'a délibérément **pas** été recalculé depuis l'archive téléchargée, ce qui reviendrait à attester ce que le serveur vient de servir. **Fragilité structurelle corrigée dans la foulée** : l'installeur résolvait la *dernière* release via `get_remote_version()` tout en épinglant un hash fixe — la panne était donc garantie au prochain tag amont, et `base.py` aurait proposé la mise à jour à chaque démarrage vers un échec certain. `checksums.json` porte désormais `brush_release` à côté de l'empreinte, et `get_remote_version()` renvoie cette version en mode release ; le tag amont n'est plus récupéré que pour *informer* qu'un bump est possible. **Écarté délibérément** : récupérer le sidecar `<asset>.sha256` amont pour vérifier le téléchargement — il provient de la même origine que l'artefact qu'il atteste, donc qui peut substituer l'un peut substituer l'autre ; ce serait échanger une garantie réelle contre du confort. L'épinglage garde l'historique de ce dépôt comme racine de confiance. Couvert par `tests/test_brush_pinning.py` (9 tests), dont un garde-fou de cohérence version/empreinte qui aurait attrapé la dérive d'origine. **(2)** `base.py` n'attrapait que les exceptions alors qu'`install()` signale ses échecs par un retour : le log affichait `✅ installed automatically` juste avant `❌ Missing`. Le succès est désormais conditionné à `engine.is_installed()`. **(3)** `--clean` oubliait `.venv_4dgs` (le plus lourd) tout en annonçant une réinitialisation complète. **(4)** Le lanceur installait 4 paquets via `pip install` sans version ; trois écrasaient le lock, et `trimesh` n'était déclaré nulle part — ce chemin détourné était sa seule voie d'entrée. `trimesh` est maintenant dans `requirements.txt`/`.lock`/`pyproject.toml`, et les 4 installs sont remplacés par un contrôle sans installation. `deptry` ajouté aux 3 sites de déclaration et exécuté pour la première fois. 447 tests pass, 1 skip.

> Résolu : **360 Extractor promu étape du pipeline, en double accès** — 2026-08-07. `PIPELINE_STEPS` 7 → 8 (`extraction360` juste après `source`, en tête du groupe PARAMÈTRES) ; `TOOL_KEYS` inchangé, `360` reste dans OUTILS — deux instances distinctes d'`Extractor360Panel`, comme Brush/Entraînement et SuperSplat/Visualiser. Nouveau drapeau `source_360` (défaut off). **Point d'architecture** : contrairement à Upscale, l'extraction 360 ne transforme pas les images sur place, elle **produit un nouveau dossier** — d'où la généralisation de `_upscaled_images_dir` en **`_pipeline_images_dir`**, canal unique du « dossier d'images courant de la chaîne » que chaque pré-étape productrice écrase et que la suivante lit. Deux canaux distincts auraient laissé les étapes diverger sur le dossier courant. Conséquence : après une extraction 360 la source est toujours un dossier d'images, donc la branche vidéo de l'Upscale (qui délègue au moteur COLMAP) est correctement court-circuitée. `Extractor360Panel` bascule droite→centre (effet de bord assumé sur l'instance OUTILS, cohérent avec Brush/SuperSplat) et gagne `get_state`/`set_state` + section `ChainConfig.extraction360`. 447 tests pass, 1 skip.

> Résolu : **suppression de presets Brush et de configurations nommées** — 2026-08-07. `delete_user_preset()` et `delete_config()` existaient et étaient déjà testés ; seul l'appelant UI manquait, donc les deux listes ne pouvaient que grossir. Bouton 🗑 à côté du 💾 du dropdown de presets, actif uniquement sur un preset *utilisateur* — garde-fou extrait en `brush_presets.is_deletable()` plutôt que dupliqué aux deux points d'appel ; un preset utilisateur masquant un intégré homonyme reste supprimable et l'intégré réapparaît. Bouton « Supprimer… » dans la fenêtre Réglages (nouveau signal `deleteRequested` → `StudioWindow.delete_config_dialog()`, même idiome que `load_config_dialog` + confirmation). **Piège i18n confirmé** : les chaînes à `{0}` doivent être passées à `tr()` **sans** défaut, sinon `LanguageManager.tr()` consomme le défaut comme argument de `.format()` et l'injecte dans le placeholder. 442 tests pass, 1 skip.

> Résolu : **trois champs orphelins du panneau Projet câblés** — 2026-08-07. `checkpoint_dest`, `export_dir` et `export_format` étaient saisissables et sérialisés mais aucun worker ne les lisait. `checkpoint_dest` atteint `BrushWorker` avec la sémantique d'origine (CHANGELOG 1.2.3) restaurée telle quelle — `<destination>/<projet>/` et `keep_only_latest=True`, paramètre que `BrushWorker` acceptait déjà mais que la GUI ne renseignait jamais. Point clé : `_latest_brush_ply()` (qui alimente Nettoyage/Export/Visualiser) résout désormais le **même** dossier via le même helper — il codait en dur `<sortie>/<projet>/checkpoints`, donc tout le chaînage post-entraînement aurait silencieusement échoué dès qu'une destination personnalisée était saisie. `export_dir`/`export_format` l'emportent sur le panneau Export pendant un run chaîné, champs vides = comportement inchangé. La liste des formats d'export, dupliquée en dur dans le panneau Projet, lit maintenant `ExportEngine.SUPPORTED_FORMATS` comme le panneau Export. Nouveau module Qt-free `app/gui/chaining_logic.py` (même découpage que `reconstruction_logic.py`) + `tests/test_chaining_logic.py` (16 tests). 436 tests pass, 1 skip.

> Résolu : **Upscale promu étape du pipeline** — 2026-08-07, chantier isolé faisant suite à la refonte Studio. `PIPELINE_STEPS` passe de 6 à 7 entrées (`upscale` entre `source` et `reconstruction`), `TOOL_KEYS` de 7 à 6 ; nouveau drapeau `upscaler_avant` (défaut **off**) exposé en tête du bloc Automatisation du panneau Projet. Réglages basculés droite→centre et `get_state`/`set_state` ajoutés (le panneau était le seul de PARAMÈTRES absent de `collect_config`/`apply_config` ; nouvelle section `ChainConfig.upscale`). **Point d'architecture** : le dossier `images/` du projet n'existe pas encore à ce stade du run (c'est `_prepare_images` qui le crée), donc l'upscale « en place » était impossible — deux routes à la place. Source *images* : `UpscaleImagesWorker` écrit dans `<sortie>/<projet>/images_upscaled`, que `_build_colmap_worker` donne ensuite à COLMAP (dossier source de l'utilisateur jamais modifié). Source *vidéo* : pas de trames à agrandir encore, les paramètres sont passés à `ColmapWorker` et le mécanisme `upscale_config` d'`engine.py` — présent mais **inatteignable depuis toute interface** jusqu'ici — agrandit les trames juste après extraction et avant l'extraction de features. Dans les deux cas les images sont agrandies avant que COLMAP ne les lise. `TestWorker` factorisé en `run_upscale_job()` partagé (nom `TestWorker` mal choisi, laissé tel quel hors périmètre) ; extraction de `_resolve_source_type()` pour qu'Upscale et Reconstruction ne classent pas la même source différemment. i18n 265 → 275 clés (`rail_tool_upscale` → `rail_step_upscale`, + `chain_upscale_before`, `run_upscale_deferred`). 422 tests pass, 1 skip, 24 deselected ; 45 tests d'intégration ; ruff clean.

> Résolu : **refonte Studio — Projet au centre, Paramètres en sous-catégorie, chaînage réel** — session 14 (2026-08-07). Rail réorganisé (« Projet » hors catégorie + sections PARAMÈTRES/OUTILS génériques). **TopBar supprimée** (`app/gui/topbar.py`) : le nom de projet n'a plus qu'une source d'affichage, le mode pipeline et Lancer/Annuler passent dans le panneau Projet, ⚙ dans la barre du bas. Réglages basculés droite→centre sur les 5 panneaux Paramètres (et par ricochet OUTILS > Brush/SuperSplat, instances des mêmes classes) ; colonne droite + filet masqués par **détection structurelle**. **Chaînage réel** Entraînement → Nettoyage → Export → Visualiser dans `_run_pipeline_step` (tranche A2-I5 : l'automatisation était voulue). Nouveau : type de source Images/Vidéo/Auto, FPS conditionnel, `detect_source_kind()` (pure, sans Qt) qui gère le cas `mixed` que `_detect_input_type` ignorait, bouton Annuler dans le LogBar, bouton Lancer sur Reconstruction. **Bugs corrigés** : molette sur les combos Réglages changeait et persistait langue/thème après un clic (`wheelEvent` ignorait la molette seulement hors focus) ; `refine_mode` avait disparu de `_build_brush_worker`/`_launch_brush` dans l'arbre de travail (restauré). Bannière permanente 4DGS (préparation de dataset uniquement — aucun entraîneur natif Apple Silicon). i18n 265 → 272 clés × 9 locales. Puis **refonte visuelle du rail** : hiérarchie statique à 3 niveaux, mécanisme de repli/dépli supprimé (`CollapseState` conservée — `logbar.py` en dépend), en-têtes de groupe passés de `QPushButton` checkables à `QLabel`, filets d'arbre en QSS pur (pas de `paintEvent`, donc pas de désynchronisation au défilement). 418 pass, 1 skip, 24 deselected — strictement au niveau d'avant chantier.

> Résolu : **audit complet + correction intégrale** — session 13 (2026-08-07), `audit-report-2026-08-07.md`. 25 findings (1 critique, 10 importants, 14 mineurs), 24 traités. **A2-C1** vérification de checksum fail-open (les installateurs rustup/Brush continuaient malgré un mismatch) → `verify_download_strict()` sur les 3 appelants. **A2-I1** filtre d'images floues mort dans la GUI → `apply_source_blur_settings()` dans `reconstruction_logic.py`. **A2-I2** `ExportEngine` seul moteur sans `validate_path()`. **A2-I3** extraction d'archive : rejet des membres symlink/hardlink. **A2-I4/I5** deux findings de l'audit du 2026-08-06 déclarés corrigés à tort (boucles d'export par vertex, `PostTrainingWorker` prétendu utilisé). **A2-I6** purge i18n 633 → 257 clés (376 orphelines, vestiges de l'UI à onglets). **A2-I7** `_prepare_images` CC 40 → éclatée en 5 méthodes. **A2-I9/I10** `combo_model` (Upscale) et `combo_layout` (360) créés mais jamais peuplés → **les deux modules OUTILS étaient inopérants depuis la GUI** ; le second a été trouvé par le test de non-régression écrit pour le premier. Nouveaux garde-fous : `tests/test_locales.py`, `tests/test_panel_contracts.py` (contrats AST des 12 panneaux, sans Qt). CI : job bandit ajouté (0 MEDIUM). 7 documents obsolètes déplacés dans `docs/archive/`. **418 tests pass, 1 skip, 24 deselected** ; ruff/mypy/bandit/pip-audit au vert ; deptry 76 → 2 signalements.

> Résolu : **bug critique de lancement pipeline + rattrapages post-refonte UI** — session 2026-07-19 (session 11). `StudioWindow.launch()` calculait le plan mais ne démarrait jamais `ColmapWorker`/`BrushWorker` depuis la refonte (Source→Reconstruction→Entraînement) — câblé (`_run_pipeline_step`). `SessionManager` (dernier projet, langue déjà fonctionnelle) rebranché sur la nouvelle architecture — tranche l'ancien point 2 (rebrancher, pas supprimer), fusion sûre de `config.json`. Lanceur renommé `CorbeauSplat.command`, affichage exhaustif par défaut rétabli (à la demande utilisateur, le mode simplifié de la session précédente est retiré). Bouton Lancer ajouté au module OUTILS Brush, bouton "Supprimer le dataset" câblé (`ColmapEngine.delete_project_content`, existait mais jamais relié), viewer Brush coché par défaut, bottom bar (version + Redémarrer/Quitter, sortis des Réglages), Réglages recentrés sur le global uniquement (Mode de build Brush déplacé vers Entraînement — bug de câblage corrigé au passage, il n'était jamais transmis au moteur ; Limitation thermique dupliquée/morte retirée). E2E Sharp vidéo ajouté (réel). Langue par défaut au premier lancement : fr→en. 385 tests, aucune régression.

> Résolu : **refonte UI complète (8 lots)** — session 2026-07-19. Interface à 11 onglets (`ColmapGUI`) remplacée par `StudioWindow` (4 zones), câblage réel des moteurs sur les 7 modules OUTILS + Nettoyage/Export/Visualiser (SuperSplat), lancement basculé (`app/cli/launcher.py`), ancienne interface supprimée (`main_window.py` + 10 `app/gui/tabs/*.py`). i18n complet (22 clés manquantes comblées, 9 locales). Fix débordement horizontal (`QFormLayout.setRowWrapPolicy`). Lancement renommé `Lancer CorbeauSplat.command` (icône Finder + terminal simplifié). 385 tests, aucune régression.

> Résolu : **dette technique de l'audit d'état des lieux** (`ETAT_DES_LIEUX.md`, préalable à la refonte UI) — session 2026-07-18. `guided_matching` réactivé (case + `set_params()`), `sequential_overlap` doté d'un widget (i18n 9 locales), mapping `blur_factor` unifié dans `app/core/params.py`, `ExportTab` morte supprimée, "Lancer Brush uniquement" passe par `BrushWorker` (logs, arrêt propre), `FourDGSTab`/`Extractor360Tab` n'instancient plus leur propre worker (point d'orchestration unique dans `main_window`). 292 tests toujours verts. Version 1.5.1, commits `74bd4d1`/`765722a`.

> Résolu : **audit SRP `ColmapEngine`** — session 2026-07-15. Construction des commandes CLI COLMAP (feature_extraction/matching/mapper/undistorter) extraite vers `app/core/colmap_commands.py` (fonctions pures) ; orchestration du pipeline inchangée dans `ColmapEngine`. 292 tests passent sans modification, vérifié en exécution réelle sur Apple Silicon. Commits `12f260f` (checkpoints Brush + bugfix SplatTransform + e2e P0-P2 + manifest allégé) et `3f8e51d` (extraction colmap_commands.py).

> Résolu : **manifest.md allégé** (détail archi renvoyé vers `graphify query`) — session 2026-07-15. **`test_export_spz_created`** revérifié (isolé + suite complète + suite e2e) : passe systématiquement, l'échec signalé était obsolète — session 2026-07-15.

> Résolu : **tests e2e réels** — sessions 2026-07-09 et 2026-07-12. P0 (Upscale, 4 tests, upscayl-bin réel), P1 (360 Extractor, 25 tests unitaires mock), P2 (Sharp image, 4 tests, Sharp réel ~142s). Voir `feature-proposal.md` pour le design P3-P5.

> Résolu : **Audit complet v2026.08.06 + correction 23 findings** — session 2026-08-06 (session 12). Audit : 2 critiques (CVE urllib3 + `delete_project_content`), 9 importants (divergences CLI, widgets orphelins, `refine_mode` non persisté, race SuperSplat, `clonefile`, Blender 4.x), 12 mineurs (code mort, artefacts). 23/23 corrigés : CLI factorisé (`_build_colmap_params`), `SourcePanel` nettoyé, `UpscalePanel` enrichi (`tta`/`compression`), `refine_mode` dans `BrushParams`, `threading.Event` SuperSplat, détection `clonefile` sans effet de bord, fallback Blender 4.x. Suppressions : `verify_imports.py`, `ply_utils.py`, `.bak`/`.sync-conflict-*`, 3 classes orphelines. Ruff clean. 389 tests, 0 régression.

> Résolu : **E2E 360 Extractor + 4DGS Phase A** — session 2026-08-06 (session 12). Installation 360Extractor (clone git dans `engines/extractor_360/src/`, dossier était vide). Création `tests/integration/test_e2e_extractor_360.py` (4 tests, mode image, `layout: ring` — le CLI 360Extractor n'accepte que `{ring, cube, fibonacci}`). Création `tests/integration/_synthetic_video.py` et `tests/integration/test_e2e_4dgs.py` (4 tests, mode COLMAP dégradé sans `.venv_4dgs`). Itération 4DGS : 1 cam × 48 frames × 1024×768 pour reconstruction COLMAP robuste. Suite complète : 389 pass, 1 skip. Marqueurs `e2e_360` et `e2e_4dgs` désélectionnés par défaut.

## Graphify

Un graphe de connaissance est maintenu dans `graphify-out/`. Pour toute question d'architecture : `graphify query "<question>"`. Hook post-commit installé → graphe rafraîchi automatiquement après chaque commit (extraction code AST ; l'extraction sémantique des docs nécessite une clé API, sinon ignorée).
