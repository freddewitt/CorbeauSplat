# CorbeauSplat — Journal de bord

**État** : Chantier **Refonte UI** terminé (8 lots livrés, tous committés sur `main`). Câblage réel moteurs + Lot 7 (bascule StudioWindow) finalisés. Interface fonctionnelle vérifiée. **Reste : validation manuelle complète utilisateur sur données réelles** (exécution effective 3 pipelines + 7 modules OUTILS). Tests : 385 pass, 1 skip, 15 deselect.

**Dernières sessions** :
- **2026-07-19 (session 10, clôture)** — Refonte UI 8 lots TERMINÉE : socle préparatoire + câblage réel 7 workers OUTILS (18c52f6), Lot 5 orchestration COLMAP-only (78ba2a8, 9 tests), i18n 22 clés (56a5588, 9 locales), Lot 7 bascule StudioWindow (619705d, suppression 11 onglets), SuperSplat réel (4de8d53), débordement QFormLayout (149111d, 18 endroits), accessibilité lancement Finder/terminal simplifié (984d465). **Tous commits sur main, 385 tests stables.** Bilan : interfaces branchées workers réels, closeEvent() sécurité SuperSplat. **Reste P0 : validation manuelle utilisateur données réelles (3 pipelines + 7 modules OUTILS), SessionManager décision technique.** manifest.md RESTE À FAIRE mis à jour.
- **2026-07-19 (session 10, suite 10)** — Accessibilité lancement app non-développeur : renommage `run.command` → `Lancer CorbeauSplat.command` (git mv, références code/tests/README maj), nouveau `app/scripts/set_launcher_icon.py` (NSWorkspace/pyobjc applique assets/icon.icns au lanceur Finder, idempotent, best-effort chaque démarrage). Terminal simplifié par défaut (3 lignes français vs bannières Xcode/Homebrew/pip) ; `--verbose`/`CORBEAU_VERBOSE=1` restaure affichage détaillé pour débogage. Prompts bloquants + erreurs réelles toujours visibles. Tests : 385 pass, 1 skip, 15 deselect. Commit `984d465`. Icône Finder vérifiée appliquée.
- **2026-07-19 (session 10, suite 9)** — Bug scroll horizontal barre droite résolu (commit 149111d). Cause : combo_mode ("Modo de Entrenamiento") dépassait 34px en espagnol. Fix : `setRowWrapPolicy(QFormLayout.RowWrapPolicy.WrapLongRows)` sur 18 QFormLayout. Vérification : 10 panneaux + Réglages, 3 langues, sans débordement. **Tous points refonte UI connus fermés côté code. Reste : P0 validation manuelle utilisateur.**
- **2026-07-19 (session 10, suite 8)** — Refonte UI terminée (8 lots complets). 7 commits cette session : 2 corrections (`6cbfb38` socle, `3e0a608` journal), 5 livraisons (`18c52f6` câblage moteurs, `78ba2a8` Lot 5 tests, `56a5588` i18n pipeline, `619705d` Lot 7 bascule StudioWindow, `4de8d53` SuperSplat). Vérification réelle : StudioWindow lancée, 13 panneaux rendus, aucune erreur. **P0 : validation manuelle utilisateur sur données réelles.**
- **2026-07-18 (session 10, suite 4)** — Refonte UI Lots 5 & 6. Lot 5 : 7 modules OUTILS (Brush≈Entraînement, SuperSplat≈Visualiser réutilisés ; sharp/upscale/splattransform/4dgs/360 nouveaux). Lot 6 : Charger/Sauvegarder config (`collect_config`/`apply_config`+`config_io`), notifications macOS (`app/core/notifications.py`, pyobjc NSUserNotificationCenter — décision utilisateur), icône erreur rail→déplie logbar, passe i18n **9 langues** (93 clés ajoutées, 610/fichier, parité vérifiée — via sous-agent). 376 pass. Reste : câblage moteurs réels (dispatch + boutons locaux + déclenchement notif) = phase Apple Silicon ; bug scroll horizontal barre droite. **Prochain : Lot 7 (nettoyage + bascule main.py) ou câblage moteurs.**
- **2026-07-18 (session 10, suite 2)** — Refonte UI Lot 3 (3a/3b/3c) : panneaux Source/Reconstruction/Entraînement (objets plain center+right dans StudioWindow), toggles chaînage + undistort bindés sur run_state (`bind_flag_checkbox`, source de vérité unique), ColmapParams/BrushParams round-trip, presets Brush (merge_presets), reprise COLMAP (`classify_resume_folder`), planner `plan_pipeline` (chaînage conditionnel testé), `launch()` (plan→breadcrumb/rail/auto-follow, logbar). +23 tests (374 pass). Reste : câbler les workers réels dans launch() = surface Apple Silicon. Commits 75643c3 (3a) / 9fd48ee (3b) / +3c. **Prochain : câblage moteurs dispatch, puis Lot 4.**
- **2026-07-18 (session 10, suite)** — Refonte UI Lot 2 (squelette PySide6 4 zones, aucun câblage moteur) : `studio_window.py` (StudioWindow, top bar/rail/centre+droite QStackedWidget/logbar), `topbar.py`, `rail.py` (13 items, OUTILS repliée par défaut, statut par étape), `logbar.py` (encapsule LogsTab), `settings_window.py` (thème/langue fonctionnels), `studio_nav.py` (PageRegistry/CollapseState, logique testable hors Qt). +9 tests (353 pass). Découverte : les widgets Qt ne sont pas testables sous le mock conftest → logique extraite dans studio_nav ; validation par smoke offscreen réel + Apple Silicon. `main.py` inchangé (bascule Lot 7). **Prochain : Lot 3 (panneaux Source/Reconstruction/Entraînement branchés moteurs, vérif Apple Silicon).**
- **2026-07-18 (session 10)** — Refonte UI, Lots 0-1. Lot 0 (audit sans code) : dette technique déjà résorbée en session 9 (guided_matching, sequential_overlap, blur_factor→ColmapParams, Brush standalone recâblé) ; écart relevé : ExportTab supprimée (pas orpheline) → Lot 4 bâtira Export sur ExportEngine/ExportWorker. Lot 1 (backend pur) : `run_state.py` (RunState observable + StepStatus), `config_io.py` (ChainConfig nommé), `brush_params.py` (surface structurée, build_command inchangé, tokens identiques), `brush_presets.py` (presets user). +52 tests. Commit 0cd50a0, non poussé. Tracker `REFONTE_UI_PROGRESS.md` créé. **Prochain : Lot 2 (squelette PySide6 4 zones, exige vérif Apple Silicon).**
