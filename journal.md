# CorbeauSplat — Journal de bord

**État** : v1.2.2 (feature destination perso checkpoints Brush, mode auto COLMAP→Brush, bugfix SplatTransform PyQt6, tests e2e P0-P2 livrés). 291 pass, 2 skip, 15 deselected, 0 fail. Working tree non committé (cumule sessions 2026-07-10 + 2026-07-12).

**Dernières sessions** :
- **2026-07-12 (session 1)** — Bugfix SplatTransform crash + e2e P0/P1/P2. Fix `_browse_input()` déstructuration tuple PyQt6 (ligne 227). P0: tests e2e upscale (4 pass, upscayl-bin). P1: 25 tests unit mock 360 extractor. P2: tests e2e Sharp image (4 pass, 142s). Fichiers créés : `_synthetic_image.py`, `test_e2e_upscale.py`, `test_extractor_360_engine.py`, `test_e2e_sharp.py`, `feature-proposal.md`. Fichiers modifiés : `splat_transform_tab.py:227`, `pyproject.toml` (marqueurs). Tests 291 pass, 0 fail. Working tree non committé. RESTE : (1) commit ; (2) P3-P5 e2e (360/Sharp vidéo/4DGS) ; (3) alléger manifest.md.
- **2026-07-10 (session 1)** — Feature destination perso checkpoints Brush. Champ optionnel « Destination des checkpoints » + Parcourir dans groupe Sortie config_tab. Vide → `<dataset>/checkpoints`. Mode COLMAP→Brush auto : checkpoints → `<dest>/<nom_projet>/` ; après succès seul .ply récent conservé (intermédiaires supprimés). Mémorisé sessions (get_state/set_state), traduit 9 locales. Décision : nettoyage « dernier checkpoint » UNIQUEMENT destination perso (flux défaut garde tous). Fichiers modifiés : config_tab.py (UI, browse_ckpt_dest, get/set, state, retranslate), main_window.py (train_brush, keep_only_latest), workers.py (param, _prune_to_latest_checkpoint), 9 locales (label_ckpt_dest, ckpt_dest_placeholder, ckpt_dest_tip), test_workers.py (+2 tests). Tests 264 pass, 0 fail, 2 skip. Working tree non committé.
