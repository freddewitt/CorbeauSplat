# CorbeauSplat — Journal de bord

**État** : v1.2.2 (feature destination perso checkpoints Brush, mode auto COLMAP→Brush). 264 tests pass (0 fail, 2 skip). GUI : champ optionnel « Destination des checkpoints » + bouton Parcourir dans onglet Entraînement. Nettoyage « dernier .ply » si destination perso renseignée. Mémorisé sessions, traduit 9 locales. Working tree non committé (changements en attente confirmation utilisateur pour commit).

**Dernières sessions** :
- **2026-07-10 (session 1)** — Feature destination perso checkpoints Brush. Champ optionnel « Destination des checkpoints » + Parcourir dans groupe Sortie config_tab. Vide → `<dataset>/checkpoints`. Mode COLMAP→Brush auto : checkpoints → `<dest>/<nom_projet>/` ; après succès seul .ply récent conservé (intermédiaires supprimés). Mémorisé sessions (get_state/set_state), traduit 9 locales. Décision : nettoyage « dernier checkpoint » UNIQUEMENT destination perso (flux défaut garde tous). Fichiers modifiés : config_tab.py (UI, browse_ckpt_dest, get/set, state, retranslate), main_window.py (train_brush, keep_only_latest), workers.py (param, _prune_to_latest_checkpoint), 9 locales (label_ckpt_dest, ckpt_dest_placeholder, ckpt_dest_tip), test_workers.py (+2 tests). Tests 264 pass, 0 fail, 2 skip. Graphify 2027 nœuds, 3662 arêtes. Working tree non committé. RESTE : (1) commit ; (2) e2e réel Sharp/Upscale/4DGS/360 ; (3) alléger manifest.md.
- **2026-07-09 (session 1)** — Amorçage complété + tests e2e réels + feature « Ouvrir le splat ». Tests e2e RÉELS pipeline COLMAP→Brush→clean→export SPZ sur scène synthétique numpy+PIL (opt-in 'pytest -m e2e', 7 pass ~21s). 9 agents .opencode/agent/ créés (config cassée opencode.json). Hook graphify post-commit + post-checkout auto-refresh. Fichiers créés : _synthetic_scene.py, test_e2e_pipeline.py, 9 agents. Fichiers modifiés : pyproject.toml (e2e marker), app/gui/main_window.py, assets/locales/ (+2 clés btn_open_splat/msg_no_ply_found), .claude/agents/ (4 : blocs Interdits). Environnement : opencv-python-headless 4.13. Pièges : cv2 mock pollution (fix install), COLMAP 2 modèles features faibles (résolu PNG multi-octave), numpy ptp() supprimé, pytest mark skipif ne décore pas fixture. Commits poussés.
